# 项目会话交接

本项目使用仓库根目录的 `HANDOFF.md` 作为跨会话的唯一交接记录，供任何 Agent 或人工协作者使用。

## 新会话开始

开始任何项目工作前，先读取 `HANDOFF.md`，将其中的当前目标、决定、待办和约束视为当前工作上下文；再根据用户最新请求继续。若交接记录缺失、过期或与仓库现状冲突，先以仓库和用户最新指令为准，并在本轮结束时修正记录。

用户说“继续项目”“读取交接”“恢复工作”时，先简短说明已恢复的目标与下一步，然后继续执行，无须要求用户重新描述背景。

## 收尾与交接

用户说“生成交接”“写 handoff”“同步项目状态”或明确准备切换会话时：

1. 检查实际文件改动、当前分支和必要验证结果。
2. 更新 `HANDOFF.md`，只保留仍影响后续工作的事实。
3. 向用户输出一份可直接粘贴到新对话的交接摘要，目标长度 1000～2000 个中文字符；信息不足时明确写“待确认”，不得臆测。

在完成任何实质性工作、且本轮产生了新的决定、未完成事项、代码状态或验证结果时，也同步更新该文件。不要记录密钥、Cookie、令牌、个人信息或冗长命令输出。

## 交接文件格式

固定使用这些标题：当前目标、已完成的工作、重要决策、待完成事项、约束与风险、当前代码状态、下一步。每项使用短句和可验证事实；“当前代码状态”应注明分支、相关文件、测试/构建结果及未提交改动（如有）。
