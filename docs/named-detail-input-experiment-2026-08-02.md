# 独立具名局部图输入实验记录（2026-08-02）

## 目标

在豆包 1.6 视觉、基线 Prompt、固定四条训练 Case 不变的前提下，比较三张原图与五张独立具名局部图共同输入是否改善细粒度问题识别。人工标签只用于本地评测，不传给模型。

## 已准备

- 三张原图被标准化为 JPEG，五张局部图按固定角色生成。
- 四条 Case 共生成 12 张原图和 20 张局部图。
- 请求映射、清单和逐条保存的实验运行器已实现。
- 成功标准：人工问题召回 4/4、额外发现少于 17、无证据角色混淆。

## 本轮结果

在逐字段删除旧文件、重新上传并核对八个文件名后，完成了两个手部训练 Case：

- Case `67ee74b4fc46c51704334675`：人工问题为手指/关节轻微瑕疵；模型四维均判 2 分、问题为空且总体置信度 1.0，未召回人工问题。
- Case `6f589d384988d3af0592bfe4`：人工问题同为手指/关节轻微瑕疵；模型命中左手手指扭曲，同时额外报告发型不符、面部变形、服装颜色错误、条纹图案模糊、背景光斑异常，共 5 条待人工核验的额外发现，并给出总体置信度 1.0。

两条合计人工问题召回 1/2，额外发现 5 条。因为第一条已经漏检，四条实验的 4/4 召回目标不可能达到，按门禁停止后续发型和 Logo Case。

Coze 上传表单确实会在连续换图时异步重排。第二条在运行前发现并修正过字段错位；最终运行前八个字段均逐项显示正确文件名，无旧 contact-sheet、空字段或角色错绑。

## 恢复状态

唯一 Coze 测试副本已再次恢复三输入基线：Start 和模型节点只保留 `user_image`、`outfit_image`、`generated_image`；临时局部图角色约束已从 Prompt 移除；模型仍为豆包 1.6 视觉，工作流未发布。

## 决策

`named-detail-v1` 不升级，也不运行剩余两条。结果说明固定裁剪有时能暴露手部异常，但不能稳定提升视觉理解，并继续伴随过度发现与错误满置信度。冻结验证集和测试集继续保持未触碰；当前仍不启用模型协作。
