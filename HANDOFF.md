# 项目交接记录

更新时间：2026-08-02

## 当前目标

以历史人工问题标注为标准答案，评估多模态模型通过 Prompt 对用户图、搭配图、生成图进行理解、比较和问题识别的能力，并建立可重复的单模型选择、Prompt 优化和冻结集回归流程。

## 已完成的工作

- 已读取 9 个飞书来源并盘点 26 个 Sheet，标准化得到 87 个唯一 Case、53 个 user+outfit 分组；61 个 Case 有问题标签。
- 已确认唯一 Coze 测试副本为工作流 `7668610423507419182`；未发布，未修改原生产链路。
- 已建立飞书只读采集、数据标准化、Coze 批量运行、标签级评测、badcase 记录和数据集切分代码。
- 已发现 Coze URL 图片首次抓取存在时序问题：表单已接受 URL 时模型仍可能看不到图片。旧三模型 10 条对比受此污染，不能用于模型排名。
- 已在批处理器中加入填值后 3.5 秒等待；若模型明确输出“输入缺失”，同输入自动复跑一次并记录 `inputWarmRetry`。
- 已验证图片 URL 本身可访问；工作流三个视觉变量均正确绑定。
- 已在豆包 1.8 深度思考上测试 evidence-first Prompt v1。修正输入后 10 条均可解析，但对人工 8 个问题标签命中 0，另有 7 个未验证额外发现；该 Prompt 不升级。
- 已按 groupId 隔离生成固定切分：训练 56 条/32 组/58 标签，验证 20 条/14 组/23 标签，测试 11 条/7 组/18 标签。
- 已从训练分区固定抽取 10 条模型比较集，覆盖手指/关节、背景变化、发型变化、Logo/图案及 2 条无问题样本，共 11 个人工问题标签。
- 已使用相同 Prompt、相同图片、相同输出格式和修正后的加载机制完成三模型公平重测。豆包 1.8：9 条有效、1 条输入失败、问题召回 0/11；豆包 2.0 mini：9 条有效、1 条超时、问题召回 0/10；豆包 1.6 视觉：10 条有效、问题召回 5/11（45.5%）、17 个未验证额外发现。
- 豆包 1.6 视觉命中手指/关节 3/3、背景变化 1/2、Logo/图案 1/2；未命中发型变化 0/2、身材差异 0/1、衣服边缘穿模 0/1。
- 已为评测器加入维度内语义标签归一化，避免“手指数量异常”和“手指/关节轻微瑕疵”等同义标签被错误算作不命中；同时单独统计输入失败。
- Coze 唯一测试副本当前已切换为豆包 1.6 视觉理解-250815并自动保存，仍未发布。
- 已确认采用“逐案例聚焦”的额外发现审查页：三张图片、人工标签和模型额外发现同时展示；每条发现独立标记为真实问题、模型误报或不确定。
- 已完成审查页书面设计：`docs/superpowers/specs/2026-08-02-extra-finding-review-design.md`；当前停在用户复核设计文档的门禁，尚未进入实现。
- 用户强调审查页只是辅助 AI 标注 Prompt 设计的一次性工具，已废弃产品化实施计划，改为单脚本生成自包含临时 HTML。
- 已生成训练集审查页：`work/extra-findings-review.html`，覆盖 5 个 Case、17 条额外发现；三张图片均可加载，页面支持本地保存和 JSON 导出。
- 已完成人工复核：17 条额外发现中 2 条真实问题、15 条误报，额外发现误报率 88.2%；真实补充问题为面部五官变形和手指/关节异常。
- 已测试 Prompt v2 balanced：前 5 条所有 issues 为空，误报下降但人工问题召回归零，不升级。
- 已测试 Prompt v2.1 targeted：前 2 条均有人工手指/关节问题，模型仍以 0.9 confidence 判正常；提前停止，不升级。
- 已将 Coze 唯一测试副本恢复到当前最佳基线 Prompt 并确认自动保存，仍未发布。
- 已实现确定性本地组合裁剪图生成、本地文件 Coze 输入和训练集实验脚本；10 个比较 Case 的 30 张组合图已成功生成。
- 已完成 4 条定向裁剪 smoke：原图基线召回 2/4、额外发现 12 条；组合裁剪图召回 4/4、额外发现 17 条。裁剪输出还出现占位描述、模糊证据和语义混淆。
- 已实现“3 张原图 + 5 张独立具名局部图”的预处理、请求映射、固定 4 Case 清单和实验运行器；本地已生成 12 张标准化原图与 20 张局部图，人工标签不会进入 Coze 请求。
- 已完成具名局部图前两条手部训练 Case：第一条漏检人工手部问题且全维度 2 分/置信度 1.0；第二条命中手部问题，但产生 5 条待核验额外发现并仍给总体置信度 1.0。两条合计召回 1/2、额外发现 5 条。
- 因第一条漏检后已不可能达到预设 4/4 召回门槛，按门禁停止后续发型与 Logo Case，`named-detail-v1` 不升级。
- 已将唯一 Coze 测试副本再次恢复为三输入基线：Start 与模型节点均只含 `user_image`、`outfit_image`、`generated_image`，局部图 Prompt 附录已移除，豆包 1.6 视觉保持不变并已自动保存，未发布。
- 已完成第二条具名局部图 Case 的专项人工复核：用户确认发型不符、面部变形、服装颜色错误、条纹图案模糊、背景光斑异常 5 条全部为误报。两条实验合计召回 1/2，问题级精确率 1/6（16.7%）。

## 重要决策

- 人工问题标签只进入本地评测器，不向被测模型披露。
- 首轮必须比较单模型；没有单模型可靠基线及错误互补证据前，不启用投票或模型协作。
- 旧的豆包 1.8、2.0 mini、1.6 视觉 10 条结果仅作输入链路诊断，不能据此选最佳模型。
- 修正后公平重测显示豆包 1.6 视觉是当前最佳单模型候选；下一阶段优先对其做 Prompt 优化，不进入模型协作。
- Prompt 调试只查看训练集标签；验证集只比较候选版本；测试集在最终候选确定前冻结。
- 模型额外发现但人工未标注的问题记录为 `unverified_extra_finding`，不直接算正式误报。
- Prompt 优化采用平衡模式；先完成人工审查，再利用已确认结论约束召回与误报，冻结验证集和测试集不进入审查页。
- Prompt 能改变报告倾向，但当前实验没有证明能改善细粒度视觉理解；不再继续堆叠提示词。
- 下一步仍保持豆包 1.6 单模型，先测试确定性的局部裁剪/放大输入；局部输入仍无增益后才讨论模型协作。
- `contact-sheet-v1` 不升级，不运行剩余 6 条，不进入冻结验证集，也不建设内部 HTTP 裁剪服务；定向召回提升不足以抵消额外发现和证据质量退化。

## 待完成事项

- 仅对豆包 1.6 视觉在训练集优化发型变化、衣服边缘穿模、细微背景变化和 Logo/图案漏检，并约束额外发现数量。
- 如继续输入链路实验，只测试原图与局部图作为独立具名图片变量，不再把多视图拼成一张图。
- 汇总原图、contact-sheet 和具名局部图三种输入的能力边界，决定下一项单模型视觉实验；具名局部图方案不再继续。
- 将人工复核后的 2 条真实补充与 15 条误报纳入训练集精确率统计。
- 用 20 条冻结验证集比较候选 Prompt；最终候选确定后只运行一次 11 条测试集。
- 单模型达到瓶颈后，分析不同模型错误是否互补；只有存在明确增益时才测试主模型+疑难复核协作。

## 约束与风险

- 不发布 Coze 工作流，不写回飞书，不绕过企业权限。
- 不加入身材/体型等历史数据没有的维度，不扩展姿势标准。
- 三个候选模型必须使用相同样本、Prompt、输出格式和加载机制，且不得看到其他模型输出。
- 当前 87 条数据规模较小，最终结论需报告置信区间和有效样本率；不能把 10 条探索结果当正式排名。
- 内部图片 URL 不在对话或交接中暴露；不得记录 Cookie、Token 或密钥。

## 当前代码状态

- 当前 PR 工作树分支：`codex/add-tryon-audit-evaluation`。
- 主要目录：`tryon-prompt-optimizer-runtime/`；当前仍为仓库未追踪目录，保留用户其他未追踪文件，不清理。
- 关键文件：`src/audit-workflow-runner.mjs`、`src/coze-string-workflow.mjs`、`src/audit-split.mjs`、`src/audit-evaluation.mjs`、`scripts/split-audit-dataset.mjs`、`AUDIT_EVALUATION.md`。
- 固定切分：`work/audit-split.json`；运行结果位于 `work/*predictions.json` 和 `work/*report.json`，已由 `.gitignore` 忽略。
- 模型比较集：`work/model-comparison-set.json`；公平重测结果为 `work/fair-*.json`。
- 新增文件：`src/audit-contact-sheet.mjs`、`scripts/build-audit-contact-sheets.mjs`、`scripts/run-audit-crop-experiment.mjs`、`docs/crop-input-experiment-2026-08-02.md` 及对应测试。
- 具名局部图相关文件：`src/audit-detail-images.mjs`、`src/audit-detail-manifest.mjs`、`scripts/build-audit-detail-images.mjs`、`scripts/run-audit-detail-experiment.mjs` 及对应测试。
- 最近验证：本分支 `npm test` 74 项全部通过；`npm run audit:build-details` 生成 4 个 Case、12 张标准化原图和 20 张局部图。
- GitHub 发布分支：`codex/add-tryon-audit-evaluation`；Draft PR：`https://github.com/chenxinran807-bot/aigc_pe_workflow/pull/1`。

## 下一步

汇总原图、contact-sheet 和具名局部图三种输入的能力边界，选择下一项单模型视觉实验。不进入验证集，也不启用模型协作。
