# 项目交接记录

更新时间：2026-08-02

## 当前目标

以历史人工问题标注为标准答案，评估多模态模型通过 Prompt 对用户图、搭配图、生成图进行理解、比较和问题识别的能力，并建立可重复的单模型选择、Prompt 优化和冻结集回归流程。

## 已完成的工作

- 已读取 9 个飞书来源并盘点 26 个 Sheet，标准化得到 87 个唯一 Case、53 个 user+outfit 分组；61 个 Case 有问题标签。
- 已确认唯一 Coze 测试副本为工作流 `7668610423507419182`；未发布，未修改原生产链路。
- 已建立飞书只读采集、数据标准化、Coze 批量运行、标签级评测、badcase 记录和数据集切分代码。
- 已发现 Coze URL 图片首次抓取存在时序问题：表单已接受 URL 时模型仍可能看不到图片。旧三模型 10 条对比受此污染，不能用于模型排名。
- 已在批处理器中加入填值后 3.5 秒等待；若模型明确输出“输入缺失”，同输入自动复跑一次并记录 `inputWarmRetry`。
- 已验证图片 URL 本身可访问；工作流三个视觉变量均正确绑定。
- 已在豆包 1.8 深度思考上测试 evidence-first Prompt v1。修正输入后 10 条均可解析，但对人工 8 个问题标签命中 0，另有 7 个未验证额外发现；该 Prompt 不升级。
- 已按 groupId 隔离生成固定切分：训练 56 条/32 组/58 标签，验证 20 条/14 组/23 标签，测试 11 条/7 组/18 标签。
- 已从训练分区固定抽取 10 条模型比较集，覆盖手指/关节、背景变化、发型变化、Logo/图案及 2 条无问题样本，共 11 个人工问题标签。
- 已使用相同 Prompt、相同图片、相同输出格式和修正后的加载机制完成三模型公平重测。豆包 1.8：9 条有效、1 条输入失败、问题召回 0/11；豆包 2.0 mini：9 条有效、1 条超时、问题召回 0/10；豆包 1.6 视觉：10 条有效、问题召回 5/11（45.5%）、17 个未验证额外发现。
- 豆包 1.6 视觉命中手指/关节 3/3、背景变化 1/2、Logo/图案 1/2；未命中发型变化 0/2、身材差异 0/1、衣服边缘穿模 0/1。
- 已为评测器加入维度内语义标签归一化，避免“手指数量异常”和“手指/关节轻微瑕疵”等同义标签被错误算作不命中；同时单独统计输入失败。
- Coze 唯一测试副本当前已切换为豆包 1.6 视觉理解-250815并自动保存，仍未发布。

## 重要决策

- 人工问题标签只进入本地评测器，不向被测模型披露。
- 首轮必须比较单模型；没有单模型可靠基线及错误互补证据前，不启用投票或模型协作。
- 旧的豆包 1.8、2.0 mini、1.6 视觉 10 条结果仅作输入链路诊断，不能据此选最佳模型。
- 修正后公平重测显示豆包 1.6 视觉是当前最佳单模型候选；下一阶段优先对其做 Prompt 优化，不进入模型协作。
- Prompt 调试只查看训练集标签；验证集只比较候选版本；测试集在最终候选确定前冻结。
- 模型额外发现但人工未标注的问题记录为 `unverified_extra_finding`，不直接算正式误报。

## 待完成事项

- 仅对豆包 1.6 视觉在训练集优化发型变化、衣服边缘穿模、细微背景变化和 Logo/图案漏检，并约束额外发现数量。
- 抽样人工复核 17 个 `unverified_extra_finding`，判断其是真实补充问题还是模型误报；在复核前不把 strict F1 当正式精确率。
- 用 20 条冻结验证集比较候选 Prompt；最终候选确定后只运行一次 11 条测试集。
- 单模型达到瓶颈后，分析不同模型错误是否互补；只有存在明确增益时才测试主模型+疑难复核协作。

## 约束与风险

- 不发布 Coze 工作流，不写回飞书，不绕过企业权限。
- 不加入身材/体型等历史数据没有的维度，不扩展姿势标准。
- 三个候选模型必须使用相同样本、Prompt、输出格式和加载机制，且不得看到其他模型输出。
- 当前 87 条数据规模较小，最终结论需报告置信区间和有效样本率；不能把 10 条探索结果当正式排名。
- 内部图片 URL 不在对话或交接中暴露；不得记录 Cookie、Token 或密钥。

## 当前代码状态

- 当前分支：`codex/tryon-audit-evaluation-loop`。
- 主要目录：`tryon-prompt-optimizer-runtime/`；当前仍为仓库未追踪目录，保留用户其他未追踪文件，不清理。
- 关键文件：`src/audit-workflow-runner.mjs`、`src/coze-string-workflow.mjs`、`src/audit-split.mjs`、`src/audit-evaluation.mjs`、`scripts/split-audit-dataset.mjs`、`AUDIT_EVALUATION.md`。
- 固定切分：`work/audit-split.json`；运行结果位于 `work/*predictions.json` 和 `work/*report.json`，已由 `.gitignore` 忽略。
- 模型比较集：`work/model-comparison-set.json`；公平重测结果为 `work/fair-*.json`。
- 最近验证：远程 `main` 工作区执行 `npm test`，64 项全部通过（含本次新增 10 项）。
- GitHub 发布分支：`codex/add-tryon-audit-evaluation`；提交 `8fed2a1`；Draft PR：`https://github.com/chenxinran807-bot/aigc_pe_workflow/pull/1`。

## 下一步

以豆包 1.6 视觉为最佳单模型候选，在训练集迭代 Prompt，优先提升发型、穿模、背景和 Logo/图案问题召回并降低额外发现；候选稳定后再进入 20 条冻结验证集，暂不测试模型协作。
